import { PathLayout } from '@layouts/index';
import { describe, expect, it } from 'vitest';

describe('PathLayout', () => {
    it('should render without crashing', () => {
        // This test is a placeholder to ensure the layout can be mounted
        // In a real test, you would mount the component and check its structure
        expect(PathLayout).toBeDefined();
    });
});
