---
import Chip from "@components/Chip.astro";
import Placeholder from "@assets/placeholder.svg";
---

<section class="py-20 bg-gray-50">
  <div class="container">
    <div class="text-center mb-16">
      <Chip> Colaboradores </Chip>
      <h2 class="text-3xl font-bold tracking-tighter sm:text-4xl mb-4">
        Entidades colaboradoras
      </h2>
      <p class="max-w-[700px] mx-auto text-muted-foreground">
        Este proyecto ha contado con la colaboración de diversas entidades
        públicas y privadas.
      </p>
    </div>
    <div
      class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-8 items-center justify-items-center mx-auto"
    >
      {
        [...Array(6)].map((_, i) => (
          <div class="bg-white p-4 rounded-lg shadow-sm h-24 w-full flex items-center justify-center">
            <Placeholder class="max-h-16 w-auto" />
          </div>
        ))
      }
    </div>
  </div>
</section>
